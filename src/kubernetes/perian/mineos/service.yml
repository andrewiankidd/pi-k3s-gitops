apiVersion: v1
kind: Service
metadata:
  name: mineos-service
  namespace: perian-mineos
spec:
  selector:
    app: mineos
  ports:
    - name: mineos-80
      protocol: TCP
      port: 80
      targetPort: 8443
  type: ClusterIP